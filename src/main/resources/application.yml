server:
  error:
    include-stacktrace: on_param
    whitelabel:
      enabled: false


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/personal-rest?useSSL=false&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update


